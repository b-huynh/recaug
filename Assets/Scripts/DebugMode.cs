using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebugMode : MonoBehaviour {
	public static bool active = false;

	void Update() {
		// if (Input.GetKeyDown(KeyCode.Alpha0)) {
		// 	active = !active;
		// 	Debug.LogFormat("[DebugMode] {0}", active);
		// }
	}
}
